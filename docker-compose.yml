version: '3.8'

services:
  nullkrypt3rs-webhook:
    build: .
    container_name: nullkrypt3rs-webhook
    ports:
      - "8080:8080"
    environment:
      # GitHub Configuration
      - GITHUB_TOKEN=${GITHUB_TOKEN}
      - GITHUB_WEBHOOK_SECRET=${GITHUB_WEBHOOK_SECRET}
      
      # LLM Configuration
      - LLM_MODEL=${LLM_MODEL:-o3-mini}
      - LLM_PROVIDER=${LLM_PROVIDER:-openai}
      
      # API Keys
      - OPENAI_API_KEY=${OPENAI_API_KEY}
      - ANTHROPIC_API_KEY=${ANTHROPIC_API_KEY}
      
      # Server Configuration
      - HOST=0.0.0.0
      - PORT=8080
    
    volumes:
      # Mount results directory to persist analysis results
      - ./results:/app/results
      
      # Optional: Mount logs directory
      - ./logs:/app/logs
    
    restart: unless-stopped
    
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8080/health"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 5s
    
    # Resource limits (adjust as needed)
    deploy:
      resources:
        limits:
          cpus: '2'
          memory: 2G
        reservations:
          cpus: '1'
          memory: 1G

# Optional: Add nginx reverse proxy for HTTPS
  # nginx:
  #   image: nginx:alpine
  #   container_name: nullkrypt3rs-nginx
  #   ports:
  #     - "443:443"
  #     - "80:80"
  #   volumes:
  #     - ./nginx.conf:/etc/nginx/nginx.conf:ro
  #     - ./ssl:/etc/nginx/ssl:ro
  #   depends_on:
  #     - nullkrypt3rs-webhook
  #   restart: unless-stopped

volumes:
  results:
  logs:

